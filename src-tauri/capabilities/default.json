{
  "$schema": "https://schema.tauri.app/capability/2",
  "identifier": "main-window",
  "description": "Minimum capability set for the local storm.dance application window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
